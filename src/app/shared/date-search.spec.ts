import { DateSearch } from './date-search';

describe('DateSearch', () => {
  it('should create an instance', () => {
    const directive = new DateSearch();
    expect(directive).toBeTruthy();
  });
});
