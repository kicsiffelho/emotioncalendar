import { EmotionSearch } from './emotion-search';

describe('EmotionSearch', () => {
  it('should create an instance', () => {
    const directive = new EmotionSearch();
    expect(directive).toBeTruthy();
  });
});
